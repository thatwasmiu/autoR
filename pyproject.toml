[project]
name = "autoR"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "openpyxl>=3.1.5",
  "tkcalendar>=1.6.1",
]

[project.optional-dependencies]
dev = [
  "pyinstaller>=6.14.2",
]

[tool.pyinstaller]
name = "autoR"

